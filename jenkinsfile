pipeline {
    agent any

    environment {
        VENV = "${WORKSPACE}/venv"
        SONAR_TOKEN = credentials('SONAR_TOKEN')
        // Remplace par ton IP locale (ipconfig)
        SONAR_HOST_URL = 'http://host.docker.internal:9000'
    }

    stages {

        stage('Checkout') {
            steps {
                git url: 'https://github.com/mohamedbelhaaj/djangotest.git', branch: 'main'
            }
        }

        stage('Create Virtualenv + Install Dependencies') {
            steps {
                sh '''
                    python3 -m venv $VENV
                    . $VENV/bin/activate
                    pip install --upgrade pip
                    pip install -r requirements.txt
                '''
            }
        }

        stage('Lint') {
            steps {
                sh '''
                    . $VENV/bin/activate
                    pip install flake8
                    flake8 . --exclude=venv --count --exit-zero --max-complexity=10 --max-line-length=127 || true
                '''
            }
        }

        stage('Run Tests') {
            steps {
                sh '''
                    . $VENV/bin/activate
                    python manage.py test
                '''
            }
        }

        stage('SonarQube Analysis') {
            steps {
                sh '''
                    echo "=== Testing SonarQube connection ==="
                    curl -s $SONAR_HOST_URL/api/system/status || echo "Cannot connect"
                    
                    echo "=== Running SonarScanner ==="
                    sonar-scanner \
                        -Dsonar.projectKey=django_project \
                        -Dsonar.sources=. \
                        -Dsonar.exclusions=venv/**,*.pyc,__pycache__/**,migrations/** \
                        -Dsonar.host.url=$SONAR_HOST_URL \
                        -Dsonar.token=$SONAR_TOKEN
                '''
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}